{
  "name": "dsa-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:editor": "npm --prefix \"code editor\" install --include=dev && npm --prefix \"code editor\" run build",
    "build": "npm run build:editor",
    "start": "node backend/server.js"
  },
  "dependencies": {
    "axios": "^1.7.5",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^7.5.0",
    "morgan": "^1.10.0"
  }
}
